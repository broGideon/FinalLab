<UserControl x:Class="FinalLab.View.Cards.ReferralView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FinalLab.View.Cards"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d" 
      Background="{DynamicResource PrimaryBackground}"
      Width="Auto" Height="Auto">

    <Grid>
        <Border BorderThickness="4" Grid.ColumnSpan="2" BorderBrush="{DynamicResource SecondaryBackground}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="38"/>
                </Grid.ColumnDefinitions>
                <ui:Button Foreground="Red" Grid.Column="1" Style="{DynamicResource ButtonBar}" Click="ButtonBase_OnClick">
                    <ui:SymbolIcon Symbol="Delete48"/>
                </ui:Button>
                <TextBlock x:Name="TxtBlock" Grid.Column="0" FontFamily="{StaticResource Inter}" FontSize="14" Text="{Binding Speciality}"  Margin="18,0,0,0" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>