<UserControl x:Class="FinalLab.View.Cards.ChoosingDoctorView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FinalLab.View.Cards"
      mc:Ignorable="d" 
      d:DesignHeight="200" d:DesignWidth="800"
      Background="{DynamicResource PrimaryBackground}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <TextBlock VerticalAlignment="Center" FontSize="16" HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryForeground}"/>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" VerticalAlignment="Center" FontSize="14" HorizontalAlignment="Left" Foreground="{DynamicResource SecondaryForeground}"/>
            <TextBlock Grid.Column="1" VerticalAlignment="Center" FontSize="14" HorizontalAlignment="Right" Foreground="{DynamicResource SecondaryForeground}"/>
        </Grid>
    </Grid>
</UserControl>
