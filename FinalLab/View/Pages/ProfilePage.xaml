<Page x:Class="FinalLab.View.Pages.ProfilePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      Background="{DynamicResource PrimaryBackground}"
      Height="520" 
      Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="3*"/>
            <RowDefinition />
            <RowDefinition/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" BorderThickness="0" Background="Transparent" MouseLeftButtonDown="MoveWindow">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="1" HorizontalAlignment="Right" Orientation="Horizontal">
                    <ui:Button Foreground="{DynamicResource PrimaryForeground}" Style="{DynamicResource ButtonBar}" Click="RollUpButton_Click">
                        <ui:SymbolIcon Symbol="LineHorizontal120"/>
                    </ui:Button>
                    <ui:Button Foreground="{DynamicResource PrimaryForeground}" Style="{DynamicResource ButtonBar}" Click="UnwrapButton_Click">
                        <ui:SymbolIcon Symbol="Maximize20"/>
                    </ui:Button>
                    <ui:Button Foreground="{DynamicResource PrimaryForeground}" Style="{DynamicResource CloseButton}" Click="CloseButton_Click">
                        <ui:SymbolIcon Symbol="Dismiss48"/>
                    </ui:Button>
                </StackPanel>
                <TextBlock
                    Grid.Column="0" 
                    FontSize="22" 
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Left" 
                    Text="Профиль" 
                    Foreground="{DynamicResource PrimaryForeground}"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="0,5,10,0">
                    <ui:TextBox Style="{DynamicResource ClearTextbox}" FontSize="14" Foreground="{DynamicResource PrimaryForeground }" >
                    </ui:TextBox>
                </Grid>
                <Grid Grid.Row="1" Margin="0,5,10,0">
                    <ui:TextBox Style="{DynamicResource ClearTextbox}" FontSize="14" Foreground="{DynamicResource PrimaryForeground }" >
                    </ui:TextBox>
                </Grid>
                <Grid Grid.Row="2" Margin="0,5,10,0">
                    <ui:TextBox  Style="{DynamicResource ClearTextbox}" FontSize="14" Foreground="{DynamicResource PrimaryForeground }">
                    </ui:TextBox>
                </Grid>
                <Grid Grid.Row="3" Margin="0,5,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="6*"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Column="0">
                        <ui:TextBox Style="{DynamicResource ClearTextbox}" FontSize="14" Foreground="{DynamicResource PrimaryForeground }">
                        </ui:TextBox>
                    </Grid>
                    <Grid Grid.Column="1" Margin="10,0,0,0" HorizontalAlignment="Left">
                        <ui:Button  Background="{DynamicResource Focusing}"  Click="RollUpButton_Click" Width="35">
                            <ui:SymbolIcon Symbol="Copy20" Foreground="White"/>
                        </ui:Button>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
        <Grid Grid.Row="2">
            <TextBlock Text="Тема приложения:" VerticalAlignment="Bottom" HorizontalAlignment="Left" FontSize="14" Foreground="{DynamicResource SecondaryForeground }"/>
        </Grid>
        <Grid Grid.Row="3">
            <ComboBox FontFamily="{StaticResource Inter}" Style="{DynamicResource ClearComboBoxStyle}" Margin="0,0,0,2" SelectedItem="{Binding SelectedRole}"/>
        </Grid>
        <Grid Grid.Row="4" Background="{DynamicResource SecondaryBackground}" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <TextBlock Text="  +7 495-539-30-00" VerticalAlignment="Center" FontSize="16" Foreground="{DynamicResource Focusing}"/>
            </Grid>
            <Grid Grid.Column="1">
                <TextBlock Text="Единая служба записи, круглосуточно    "  VerticalAlignment="Center" FontSize="16" HorizontalAlignment="Right" Foreground="{DynamicResource PrimaryForeground}"/>
            </Grid>
        </Grid>
        <Grid Grid.Row="5" Background="{DynamicResource SecondaryBackground}" Margin="0,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="6*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <TextBlock Text="  +7 495 051" VerticalAlignment="Center" FontSize="16" Foreground="{DynamicResource Focusing}"/>
            </Grid>
            <Grid Grid.Column="1">
                <TextBlock Text="Единая служба психологической помощи, круглосуточно.Или &#xA;                                    позвоните по городскому на номер 051" VerticalAlignment="Center" FontSize="16" HorizontalAlignment="Right" Foreground="{DynamicResource PrimaryForeground}"/>
            </Grid>
        </Grid>
        <Grid Grid.Row="6" VerticalAlignment="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" Margin="10,0,10,0">
                <ui:Button FontFamily="{StaticResource Inter}" FontSize="14" Style="{DynamicResource FocusingButton}" Padding="0, 10"  Margin="0,0,0,5" Content="Сохранить изменения" VerticalAlignment="Center">
                </ui:Button>
            </Grid>
            <Grid Grid.Column="1" Margin="10,0,10,0">
                <ui:Button FontFamily="{StaticResource Inter}" FontSize="14" Style="{DynamicResource FocusingButton}" Padding="0, 10"  Margin="0,0,0,5" Content="Добавить новый аккаунт" VerticalAlignment="Center">
                </ui:Button>
            </Grid>
            <Grid Grid.Column="2" Margin="10,0,10,0">
                <ui:Button FontFamily="{StaticResource Inter}" FontSize="14" Style="{DynamicResource AttentionButton}" Padding="0, 10"  Margin="0,0,0,5" Content="Выйти из аккаунта" VerticalAlignment="Center">
                </ui:Button>
            </Grid>
        </Grid>


    </Grid>
</Page>
